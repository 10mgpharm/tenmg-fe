export enum PRODUCTVIEW {
    LIST = "list",
    GRID = "grid",
}

export enum STEPS {
    DETAIL = "detail",
    ESSENTIAL = "essential",
    INVENTORY = "inventory",
}

export enum SETUPTYPE {
    TYPE = "type",
    CATEGORIES = "categories",
    BRANDS = "brands",
    STRENGTH="strength",
    PRESENTATION = "presentation",
    PACKAGE="package"
}
  